docker_app: docker-compose.yml

flocker_hub:
    endpoint: https://data.flockerhub.clusterhq.com

volumes:
    - name: rethink-data
      snapshot: <INSERT_SNAPSHOT>
      volumeset: <INSERT_VOLUMESET>