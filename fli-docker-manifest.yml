docker_app: docker-compose.yml

flocker_hub:
    endpoint: http://data.flockerhub.clusterhq.com
    tokenfile: /root/token.txt

volumes:
    - name: rethink-data
      snapshot: <INSERT_SNAPSHOT>
      volumeset: <INSERT_VOLUMESET>